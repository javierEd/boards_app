type BoardObject {
	id: ID!
	user: UserObject!
	name: String!
	slug: String!
	description: String!
	visibility: BoardVisibility!
	lists(after: UUID, first: Int): ListObjectConnection!
	isEditable: Boolean!
	createdAt: DateTime!
	updatedAt: DateTime
}

type BoardObjectConnection {
	"""
	Information to aid in pagination.
	"""
	pageInfo: PageInfo!
	"""
	A list of edges.
	"""
	edges: [BoardObjectEdge!]!
	"""
	A list of nodes.
	"""
	nodes: [BoardObject!]!
}

"""
An edge in a connection.
"""
type BoardObjectEdge {
	"""
	The item at the end of the edge
	"""
	node: BoardObject!
	"""
	A cursor for use in pagination
	"""
	cursor: String!
}

input BoardParams {
	name: String!
	slug: String!
	description: String!
	visibility: BoardVisibility!
}

enum BoardVisibility {
	PRIVATE
	USERS
	PUBLIC
}

type CardObject {
	id: ID!
	listId: UUID!
	content: String!
	position: Int!
	createdAt: DateTime!
	updatedAt: DateTime
}

type CardObjectConnection {
	"""
	Information to aid in pagination.
	"""
	pageInfo: PageInfo!
	"""
	A list of edges.
	"""
	edges: [CardObjectEdge!]!
	"""
	A list of nodes.
	"""
	nodes: [CardObject!]!
}

"""
An edge in a connection.
"""
type CardObjectEdge {
	"""
	The item at the end of the edge
	"""
	node: CardObject!
	"""
	A cursor for use in pagination
	"""
	cursor: String!
}

input CardParams {
	listId: UUID!
	content: String!
}

"""
Implement the DateTime<Utc> scalar

The input/output is a string in RFC3339 format.
"""
scalar DateTime

type InfoObject {
	builtAt: DateTime!
	version: String!
}

type ListObject {
	id: ID!
	name: String!
	position: Int!
	cards(after: UUID, first: Int): CardObjectConnection!
	createdAt: DateTime!
	updatedAt: DateTime
}

type ListObjectConnection {
	"""
	Information to aid in pagination.
	"""
	pageInfo: PageInfo!
	"""
	A list of edges.
	"""
	edges: [ListObjectEdge!]!
	"""
	A list of nodes.
	"""
	nodes: [ListObject!]!
}

"""
An edge in a connection.
"""
type ListObjectEdge {
	"""
	The item at the end of the edge
	"""
	node: ListObject!
	"""
	A cursor for use in pagination
	"""
	cursor: String!
}

input ListParams {
	boardId: UUID!
	name: String!
}

type MutationRoot {
	createBoard(params: BoardParams!): BoardObject!
	createCard(params: CardParams!): CardObject!
	createList(params: ListParams!): ListObject!
	createSession(params: SessionParams!): SessionObject!
	createUser(params: UserParams!): UserObject!
	deleteBoard(id: UUID!): Boolean!
	deleteCard(id: UUID!): Boolean!
	deleteList(id: UUID!): Boolean!
	finishSession: Boolean!
	updateBoard(id: UUID!, params: BoardParams!): BoardObject!
	updateCardList(id: UUID!, listId: UUID!, position: Int!): CardObject!
	updateCardPosition(id: UUID!, position: Int!): CardObject!
	updateList(id: UUID!, params: UpdateListParams!): ListObject!
	updateListPosition(id: UUID!, position: Int!): ListObject!
}

"""
ISO 8601 calendar date without timezone.
Format: %Y-%m-%d

# Examples

* `1994-11-13`
* `2000-02-24`
"""
scalar NaiveDate

"""
Information about pagination in a connection
"""
type PageInfo {
	"""
	When paginating backwards, are there more items?
	"""
	hasPreviousPage: Boolean!
	"""
	When paginating forwards, are there more items?
	"""
	hasNextPage: Boolean!
	"""
	When paginating backwards, the cursor to continue.
	"""
	startCursor: String
	"""
	When paginating forwards, the cursor to continue.
	"""
	endCursor: String
}

type QueryRoot {
	board(idOrSlug: String!): BoardObject
	boards(after: UUID, first: Int): BoardObjectConnection!
	currentUser: UserObject
	info: InfoObject!
	list(id: ID!): ListObject
	user(username: String!): UserObject
}

type SessionObject {
	id: ID!
	token: String!
	expiresAt: DateTime!
	createdAt: DateTime!
	updatedAt: DateTime
}

input SessionParams {
	usernameOrEmail: String!
	password: String!
}

"""
A UUID is a unique 128-bit number, stored as 16 octets. UUIDs are parsed as
Strings within GraphQL. UUIDs are used to assign unique identifiers to
entities without requiring a central allocating authority.

# References

* [Wikipedia: Universally Unique Identifier](http://en.wikipedia.org/wiki/Universally_unique_identifier)
* [RFC4122: A Universally Unique Identifier (UUID) URN Namespace](http://tools.ietf.org/html/rfc4122)
"""
scalar UUID

input UpdateListParams {
	name: String!
}

type UserObject {
	id: ID!
	username: String!
	initials: String!
	displayName: String!
	boards(after: UUID, first: Int): BoardObjectConnection!
	createdAt: DateTime!
	updatedAt: DateTime
}

input UserParams {
	username: String!
	email: String!
	password: String!
	fullName: String!
	birthdate: NaiveDate
	countryCode: String!
}

"""
Directs the executor to include this field or fragment only when the `if` argument is true.
"""
directive @include(if: Boolean!) on FIELD | FRAGMENT_SPREAD | INLINE_FRAGMENT
"""
Directs the executor to skip this field or fragment when the `if` argument is true.
"""
directive @skip(if: Boolean!) on FIELD | FRAGMENT_SPREAD | INLINE_FRAGMENT
"""
Provides a scalar specification URL for specifying the behavior of custom scalar types.
"""
directive @specifiedBy(url: String!) on SCALAR
schema {
	query: QueryRoot
	mutation: MutationRoot
}

