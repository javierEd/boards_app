query Board($idOrSlug: String!) {
    board(idOrSlug: $idOrSlug) {
        ...BoardFragment
        user {
            id
            identityUser {
                id
                username
            }
        }
    }
}
